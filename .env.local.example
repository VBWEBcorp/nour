# ─────────────────────────────────────────────────────────────
# Envoi d'email du formulaire de contact (SMTP de la boîte BYS)
# Copier ce fichier en `.env.local` et remplir avec les vrais identifiants.
# ─────────────────────────────────────────────────────────────

# Serveur SMTP du fournisseur de la boîte email :
#   OVH        -> ssl0.ovh.net           (port 465)
#   Gmail      -> smtp.gmail.com          (port 465, mot de passe d'application requis)
#   o2switch   -> <domaine>.o2switch.net  (port 465)
#   Outlook    -> smtp-mail.outlook.com   (port 587)
SMTP_HOST=ssl0.ovh.net
SMTP_PORT=465

# Identifiants de la boîte (adresse complète + mot de passe / mot de passe d'application)
SMTP_USER=contact@bys-consulting.fr
SMTP_PASS=

# Optionnel — expéditeur affiché (par défaut = SMTP_USER).
# Doit rester la boîte authentifiée pour ne pas partir en spam.
# SMTP_FROM="BYS Consulting <contact@bys-consulting.fr>"

# Optionnel — boîte qui reçoit les messages (par défaut = SMTP_USER)
# CONTACT_TO=contact@bys-consulting.fr
